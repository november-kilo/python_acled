Parse CSV data from https://acleddata.com/data-export-tool/ to KML.
Also prints summaries.

Example usage:
python main.py -d data/2020-04-01-2020-04-30.csv -f 0 -k -s event_type_count battle_fatalities -t "April 2020"

Run tests:
python -m unittest
